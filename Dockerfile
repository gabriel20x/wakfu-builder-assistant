# This file should not be used directly
# If you see this, it means Render is not using render.yaml correctly
# Use render.yaml Blueprint deployment instead

# For API, see: ./api/Dockerfile.prod
# For Frontend, see: ./frontend/Dockerfile.prod
# For Worker, see: ./worker/Dockerfile

# Render Blueprint Configuration:
# Services should be built from their respective dockerfiles as configured in render.yaml
